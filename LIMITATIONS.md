# 模型边界与已知问题（LIMITATIONS.md）

本文件集中说明模型做不到什么、哪些结论不能超出证据范围引用，以及当前已知的问题。

---

## 一、适用范围

### 1. 机理模型，不含真实数据

所有脚本未读取任何实验或组学数据文件，输入为参数，输出为数值积分结果。
模型回答的是"在给定假设下，该设计在动力学上是否自洽"，
不回答"该设计在真实生物系统中是否成立"。

### 2. 参数未经实验标定

`PARAMETERS.md` 中多数参数为量级估计或设计取值，无实测值。
结论对参数的依赖关系已部分通过扫描检验，但参数本身未标定。

### 3. 结论均未经实验验证

各模型结论对应的最小验证实验如下：

| 模型结论 | 所需实验 |
|---|---|
| 周期性裂解振荡（周期约 4 h） | OD600 长时间连续监测，观察 3 个以上下降-恢复周期 |
| 乳酸阈值约 5–6 mM | 乳酸梯度下的生长-裂解曲线 |
| lacO ≥ 3 兼顾低泄露与可激活 | 无诱导/诱导条件下的裂解蛋白定量（Western blot） |
| 安全窗口存在 | 低乳酸条件下的 CFU 生存曲线 + DAP 回补实验 |
| 脉冲优于持续 | 荷瘤小鼠等剂量对照实验 |

---

## 二、简化假设

| # | 假设 | 位置 | 说明 |
|---|---|---|---|
| 1 | 裂解后 `E` 直接重置为 0 | `src/01` `src/02` | 以"高 E 细胞被清除、残存群体重新表达"的群体平均近似 |
| 2 | 裂解后存活比例固定（0.2 / 0.25） | `src/01` `src/02` | 真实存活率依赖空间位置、生长状态与表达异质性 |
| 3 | DAP 低于临界值后细菌 5 分钟内全部清除 | `src/05` | 代码中写死 `B[t > t_DAP_crit + 5] = 0`，属示意性处理，非动力学推导 |
| 4 | 裂解为瞬时事件 | `src/01` `src/02` | 真实裂解有持续时间与异步性；`src/02` 以 Depot 项（`k_abs`）部分缓解尖峰 |
| 5 | 蛋白衰减为一级动力学、单一速率 | `src/05` | 未考虑 ClpAP 饱和及生长稀释对降解的贡献 |
| 6 | 肿瘤为单一均质隔室 | `src/02` | 未建模定植的空间异质性、乳酸梯度与药物扩散 |
| 7 | 纳米抗体为单室 PK | `src/02` | 未建模组织穿透与靶点介导清除（TMDD） |
| 8 | T 细胞活性 `A` 为抽象标量 | `src/02` | 非免疫细胞群体动力学，未区分 CD8⁺/Treg/MDSC |
| 9 | 细菌生长为 Logistic、无空间限制 | `src/01` `src/02` | 未建模肿瘤内营养竞争与坏死核心 |
| 10 | 乳酸浓度在模拟期间恒定 | `src/03` | 见下方说明 |

第 10 条是当前最实质的空缺：`src/03` 将乳酸作为外部常量扫描，
而项目设计本身包含"细菌消耗乳酸"的过程（LldR 自调控即为防止过度消耗而设）。
因此现有模型无法验证 LldR 自调控是否有效，只给出静态阈值。
乳酸消耗与振荡的闭环模型为待办事项中优先级最高的一项。

---

## 三、已知问题

### 3.1 安全窗口图的两个面板使用了不同物理量

`src/05_safety_window_robustness.py` 的两个子图对"安全窗口"的第二个事件定义不同：

| | 第一个事件 | 第二个事件 | 速率 | 数值结果 |
|---|---|---|---|---|
| 左图（衰减曲线） | `t_E_safe`（E 降到阈值） | `t_DAP_crit`（DAP 降到 0.2） | `gamma_DAP_eff = 0.12` | 8.6 → 38.4 min，窗口 ≈ 30 min |
| 右图（鲁棒性扫描） | `t_lysis_loss`（E 降到阈值） | `t_asd_depletion`（Asd 降到 10%） | `beta`（随半衰期变化） | 半衰期 7 min 时 9.5 → 24.0 min，窗口 14.4 min |

问题有三处。其一，第二个事件一处用 DAP（细胞壁成分，直接死因）、一处用 Asd（上游酶），
二者时间尺度不同。其二，同一张图的两个面板给出的窗口数值相差约一倍（约 30 与 14.4 min）。
其三，左图初始裂解蛋白取 `E_0 = 18.82`，右图取 `E_tumor_ss = 20.0`，初值不一致。

`docs/建模目的.docx` 中答辩稿写的是左图口径（约 30 分钟），代码打印的是右图口径（14.4 分钟），
两处对外表述不一致。待统一为同一物理量。从机制上讲，死亡的直接原因是 DAP 耗尽，
更严格的做法是建模为"Asd 以 `beta` 衰减 → DAP 合成停止 → DAP 池以 `gamma_DAP_eff` 衰减"
的两级串联，而非二者混用。

### 3.2 lacO 结论的适用区间

`src/04_laci_laco_damper.py` 的实际运行输出：

```
lacO = 0: 无 IPTG → E = 0.15 a.u. (安全)   |   IPTG → E =  7.57 a.u. (窗口外)
lacO = 1: 无 IPTG → E = 0.08 a.u. (安全)   |   IPTG → E =  9.67 a.u. (窗口外)
lacO = 2: 无 IPTG → E = 0.05 a.u. (安全)   |   IPTG → E = 11.09 a.u. (窗口外)
lacO = 3: 无 IPTG → E = 0.04 a.u. (安全)   |   IPTG → E = 12.05 a.u. (最优)
lacO = 4: 无 IPTG → E = 0.03 a.u. (安全)   |   IPTG → E = 12.71 a.u. (最优)
lacO = 5: 无 IPTG → E = 0.02 a.u. (安全)   |   IPTG → E = 13.18 a.u. (最优)
```

lacO = 3、4、5 均落在最优窗口内，且位点数越多表达水平越高。
模型支持的结论是"lacO ≥ 3 即可同时满足低泄露与可激活"，
而非 3 为最优。本项目取 3 的依据是元件极简原则——
在满足功能的前提下取最少元件数，与仅 2–3 个外源基因的整体设计一致，

### 3.3 随机模拟模块对应早期设计

`src/06_stochastic_individual_based.py` 含 `S_prod` / `S_threshold`（群体感应 AHL 信号），
建模的是早期 QS 依赖方案；最终设计为无 QS 依赖的弛豫振荡。
因此该模块不能用于支持最终设计的随机性质。
原文件名为 `Gillespie.py`，实际实现的是固定步长随机个体模型而非 Gillespie SSA
（见该文件头部说明）；`legacy/` 下两个随机模型脚本同理。

### 3.4 LacI-lacO 两组情景使用了方向相反的 K50 公式

`src/04_laci_laco_damper.py`：

```python
K50_eff_green = K50_base * (1 + N_lacO_vals / K_N)   # 位点越多 → K50 越大 → 抑制越弱
K50_eff_red   = K50_base / (1 + N_lacO_vals / K_N)   # 位点越多 → K50 越小 → 抑制越强
```

两个情景（有无 IPTG）使用不同函数形式，方向相反，缺乏明确的物理依据，
无法排除是为了让曲线形状符合预期而分别设定。待统一为单一物理模型
（例如 lacO 数量 → 有效抑制位点数 → 表观 K50），或说明两种情景的机制差异。

### 3.5 表达窗口口径不一致

`docs/建模目的.docx` 中有效表达窗口为 12–15 a.u.，
`src/04` 代码中的窗口定义为 1.5×阈值 至 2.5×阈值（即 12–20 a.u.）。两处口径待统一。

### 3.6 已修复：临界半衰期的输出逻辑

原代码以 `np.argmax(safety_window < 0)` 定位临界半衰期。当扫描范围内窗口始终为正时，
`argmax` 返回 0，代码转而取 `half_lives[-1]`，打印"临界半衰期: 60.0 分钟（安全窗口归零）"——
与实际含义（范围内从未归零）相反。已改为以 `np.where(safety_window < 0)` 判断，
区分"存在归零点"与"范围内未归零"两种情况分别输出。修改已同步至归档版与原始脚本。

### 3.7 其他

- `src/06` 使用固定步长 `dt = 0.01 h`，未做步长收敛性检验（`dt` 减半后统计量是否变化）。
- `src/01`、`src/03`、`src/04`、`src/05` 无 `if __name__ == '__main__'` 保护，导入即执行。
- 输出依赖 `plt.show()`，未统一以 `plt.savefig()` 写入 `figs/`（`figs/` 下为历史输出）。

---

## 四、结论的表述边界

| 结论 | 不准确的表述 | 与证据相符的表述 |
|---|---|---|
| 振荡 | 工程菌能稳定振荡 | 在给定参数组合下，模型支持回路具备产生稳定振荡的动力学可行性 |
| 脉冲 vs 持续 | 脉冲给药疗效更好 | 等总剂量下，模型中脉冲组的相对 TGI 高于持续组（58.6% vs 39.0%） |
| 乳酸阈值 | 5 mM 以上就能启动 | 模型预测 ALPaGA 约 5 mM 激活、5–6 mM 进入稳定振荡区，且存在稳态陷阱 |
| lacO | 3 个 lacO 最优 | 有效区间为 lacO ≥ 3；取 3 依据元件极简原则 |
| 安全窗口 | 系统在体内是安全的 | 模型支持"先关闭裂解、后清除细菌"的时序逻辑，且对蛋白半衰期鲁棒 |

`docs/建模目的.docx` 与申报书中出现的下列数字均为模型输出，非实验结论，
引用时应注明未经实验验证：

- "基础泄露降至 2.9% 以下"
- "正常器官细菌载量降低 1–3 个数量级"
- "脉冲组的肿瘤抑制率比持续组高出 50%"（对应 58.6% vs 39.0% 的相对比值）
- "将剩余 95% 的 pMMR/MSS 冷肿瘤转变为热肿瘤"（愿景表述，非模型结论）

---

## 五、可证伪条件

以下实验结果可以推翻本模型的对应结论：

1. OD600 长时间监测观察不到 3 个以上连续的下降-恢复周期——弛豫振荡机制不成立；
2. 乳酸梯度实验中不存在"激活但不裂解"的中间区间——稳态陷阱不存在，LldR 自调控的必要性下降；
3. 改变 lacO 位点数不改变泄露水平——LacI-lacO 阻尼设计无效；
4. 低乳酸条件下细菌存活时间不短于野生型——asd 平衡致死系统未生效；
5. 等剂量荷瘤实验中脉冲组与持续组无差异——PK/PD-免疫节律模型的核心假设错误。

---

## 六、待办

- 按元件极简原则的口径统一申报书与文档中关于 lacO 的表述（3.2）
- 补齐 `PARAMETERS.md` 的文献出处（至少 `nb_EC50`、`ALPaGA EC50`、`Ki`、`k_death`）
- 统一 `src/04` 的 K50 公式与表达窗口口径（3.4、3.5）
- 乳酸消耗与振荡的闭环模型（二-10，当前最大的模型空缺）
- 为最终弛豫振荡设计补写随机模拟版本（3.3）
- `src/06` 步长收敛性检验；统一 `plt.savefig()` 输出；补 `__main__` 保护（3.7）

---

## 七、修改记录

| 日期 | 内容 | 位置 |
|---|---|---|
| 2026-09-17 | 删除图表缩放系数 `scale_factor = 0.85`，改为同指标内直接归一化 | `legacy/drug_efficacy_v3_simple.py` |
| 2026-09-17 | 指标名 `肿瘤抑制率` 改为 `肿瘤回缩率`（相对初始值而非无治疗对照）；`np.mean` 改为时间加权均值 | `legacy/drug_efficacy_v3_simple.py` |
| 2026-09-17 | 参数注释由目标导向改写为取值依据说明 | `src/02_pkpd_immune_rhythm.py` |
| 2026-09-17 | 修复临界半衰期输出逻辑（3.6） | `src/05_safety_window_robustness.py` |
| 2026-09-17 | 更正 `Gillespie` 命名并补文件头方法说明 | `src/06_stochastic_individual_based.py` |

修改前的原始内容见 [`HISTORY.md`](HISTORY.md) 的「归档时修改的代码内容」一节。
