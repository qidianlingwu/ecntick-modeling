# 模型边界与已知缺陷（LIMITATIONS.md）

> **这份文件的目的**：把所有"这个模型做不到什么"集中写清楚。
> 建模最容易犯的错不是算错，而是**把模型的结论说得比它能支撑的更强**。
> 本文档按"严重程度"排序，🔴 表示必须在对外表述时主动说明。

---

## 一、根本性边界（🔴 必须主动说明）

### 1. 全部为机理模型，不含任何真实数据

所有脚本**没有读取任何一个实验或组学数据文件**。输入是参数，输出是数值积分结果。
因此本模型能回答的是"**在给定假设下，这个设计在动力学上是否自洽**"，
而不能回答"**这个设计在真实生物系统里会不会成立**"。

**对外应说**：*"这是一个机理模型，用来验证设计在动力学上是否自洽，不是数据分析。"*

### 2. 参数无实验标定

`PARAMETERS.md` 中大量参数标注为 🟡 量级 或 🔴 待补。
模型结论对参数的依赖关系已部分通过扫描检验，但**参数本身不是实测值**。

**风险**：若被问"你的 EC50 = 2e6 怎么来的"，**不能**回答"实验测的"。

### 3. 没有任何实验验证

所有结论均待实验检验。最小验证方案：

| 模型结论 | 需要什么实验 |
|---|---|
| 能周期性裂解振荡（周期约 4 h） | OD600 长时间连续监测，观察 3 个以上下降-恢复周期 |
| 乳酸阈值约 5–6 mM | 乳酸梯度下的生长-裂解曲线 |
| 3×lacO 兼顾低泄露与可激活 | 无诱导/诱导条件下的裂解蛋白定量（Western blot） |
| 安全窗口存在 | 低乳酸条件下的 CFU 生存曲线 + DAP 回补实验 |
| 脉冲优于持续 | 荷瘤小鼠等剂量对照实验 |

---

## 二、模型简化假设（🔴 其中若干条容易被追问）

| # | 假设 | 位置 | 为什么是简化 |
|---|---|---|---|
| 1 | 裂解后 `E` 直接重置为 0 | `src/01` `src/02` | 实际是"高 E 细胞被清除、残存群体重新表达"，用群体平均近似 |
| 2 | 裂解后存活比例固定（0.2 / 0.25） | `src/01` `src/02` | 真实存活率依赖空间位置、生长状态、表达异质性，应实验标定 |
| 3 | DAP 低于临界值后细菌 5 分钟内全部清除 | `src/05` | **代码中直接写死 `B[t > t_DAP_crit + 5] = 0`**。这是最粗的一处近似，属于示意性处理，不是动力学推导 |
| 4 | 裂解是瞬时事件 | `src/01` `src/02` | 真实裂解有持续时间与异步性；`src/02` 用 Depot（`k_abs`）部分缓解了尖峰问题 |
| 5 | 蛋白衰减为一级动力学、单一速率 | `src/05` | 未考虑 ClpAP 饱和、生长稀释对降解的贡献 |
| 6 | 肿瘤为单一均质隔室（无空间结构） | `src/02` | 未建模细菌定植的空间异质性、乳酸梯度、药物扩散 |
| 7 | 纳米抗体为单室 PK | `src/02` | 未建模组织穿透、靶点介导清除（TMDD） |
| 8 | T 细胞活性 `A` 是抽象标量 | `src/02` | 不是真实的免疫细胞群体动力学，未区分 CD8⁺/Treg/MDSC |
| 9 | 细菌生长为 Logistic，无空间限制 | `src/01` `src/02` | 未建模肿瘤内的营养竞争与坏死核心 |
| 10 | 乳酸浓度在模拟期间恒定 | `src/03` | 而项目设计恰恰包含"细菌消耗乳酸"这一过程 —— 模型未闭环 |

> **第 10 条最值得注意**：`src/03` 把乳酸当作外部常量扫描，
> 但整个项目的核心创新点是"用 LldR 自调控防止乳酸被过度消耗"。
> 也就是说，**该模型无法验证 LldR 自调控本身是否有效** —— 它只给出静态阈值。
> 这是目前最实质的一处模型空缺。

---

## 三、代码层面的已知问题（🟡 需要主动了解）

### 1. 随机模拟模块建模的是被放弃的设计

`src/06_stochastic_individual_based.py` 含 `S_prod` / `S_threshold`（群体感应 AHL 信号），
建模的是**早期 QS 依赖方案**；而最终设计是**无 QS 依赖的弛豫振荡**。

- 因此该模块**不能**用来支持最终设计的随机性质；
- 原文件名为 `Gillespie.py`，但**实现的不是 Gillespie SSA**（详见该文件头部说明）；
- `legacy/stochastic_ibm_v1.py`、`legacy/stochastic_ibm_v2_soft_capacity.py` 同理。

**待办**：为最终的弛豫振荡模型写一个随机版本（可用真 SSA 或 tau-leaping）。

### 2. 时间步长未做收敛性检验

`src/06` 用固定 `dt = 0.01` h。**未见对 `dt` 的收敛性测试**
（例如 `dt` 减半后结果统计量是否变化）。
这是随机模拟的基本验证步骤，目前缺失。

### 3. LacI-lacO 的红/绿两组用了方向相反的 K50 公式

`src/04_laci_laco_damper.py`：

```python
K50_eff_green = K50_base * (1 + N_lacO_vals / K_N)   # 位点越多 → K50 越大 → 抑制越弱
K50_eff_red   = K50_base / (1 + N_lacO_vals / K_N)   # 位点越多 → K50 越小 → 抑制越强
```

两套公式**方向相反**。代码注释称后者是"修正"，
但两个情景（有无 IPTG）使用不同函数形式需要**明确的物理依据**。
目前无法排除二者只是为了让曲线形状符合预期而分别设定。

**待办**：统一为一个物理模型（例如 lacO 数量 → 有效抑制位点数 → 表观 K50），
或写清两种情景的机制差异。

### 4. 表达窗口口径不一致

- `docs/建模目的.docx`：诱导条件下有效表达窗口为 **12–15 a.u.**
- `src/04` 代码：最优窗口定义为 **1.5×阈值 ~ 2.5×阈值 = 12–20 a.u.**

**待办**：对外表述前统一口径。

### 5. `src/01`、`src/03`、`src/04`、`src/05` 无 `if __name__ == '__main__'` 保护

模块导入即执行 + `plt.show()`，不便复用与测试。
`src/02`、`src/06` 已具备（`src/02` 有 main guard，`src/06` 无但仍会写文件）。

### 6. 输出依赖 `plt.show()`，未统一保存到 `figs/`

目前脚本弹窗显示，`figs/` 下的图是历史输出。
建议统一改为 `plt.savefig(f'../figs/xxx.png', dpi=150, bbox_inches='tight')`。

---

### 7. 🔴 安全窗口图的两个面板用了**不同的物理量**，数值差近一倍

`src/05_safety_window_robustness.py` 的两个子图，对"安全窗口"的第二个事件
采用了**不同的定义**：

| | 第一个事件 | 第二个事件 | 速率 | 数值结果 |
|---|---|---|---|---|
| **左图**（衰减曲线） | `t_E_safe`（E 降到阈值） | **`t_DAP_crit`（DAP 降到 0.2）** | `gamma_DAP_eff = 0.12` | 8.6 → **38.4** min，窗口 ≈ **30 min** |
| **右图**（鲁棒性扫描） | `t_lysis_loss`（E 降到阈值） | **`t_asd_depletion`（Asd 降到 10%）** | `beta`（随半衰期变化） | 按半衰期 7 min 计：9.5 → **24.0** min，窗口 = **14.4 min** |

**问题**：
1. 第二个事件一处用 **DAP**（细胞壁成分，直接死因），一处用 **Asd**（上游酶）。
   二者是不同的物理量，时间尺度也不同（`gamma_DAP_eff` vs `beta`）。
2. 因此**同一张图里两个面板报告的"安全窗口"数值差了约一倍**（≈30 vs 14.4 min）。
3. 左图初始裂解蛋白用 `E_0 = 18.82`，右图用 `E_tumor_ss = 20.0`，也不一致。

**`docs/建模目的.docx` 的答辩稿写的是"约 8.6 分钟 → 约 38.4 分钟，安全窗口约 30 分钟"**
（对应左图口径），而代码实际打印的是右图口径的 9.5 / 24.0 / 14.4。**两处对外口径不一致。**

**待办（需要你做科学判断，不宜代改）**：统一为同一个物理量。
从机制上看，细菌死亡的直接原因是 **DAP 耗尽**，而 Asd 耗尽只是上游事件；
若要严格建模，应写成"Asd 以 `beta` 衰减 → DAP 合成停止 → DAP 池以 `gamma_DAP_eff` 衰减"的两级串联，
而不是二者混用。

---

### 8. 🔴 "3×lacO 是最优设计"这一结论被**过度解读**

`src/04_laci_laco_damper.py` 的实际运行输出：

```
lacO = 0: 无 IPTG → E = 0.15 a.u. (安全)   |   IPTG → E =  7.57 a.u. (窗口外)
lacO = 1: 无 IPTG → E = 0.08 a.u. (安全)   |   IPTG → E =  9.67 a.u. (窗口外)
lacO = 2: 无 IPTG → E = 0.05 a.u. (安全)   |   IPTG → E = 11.09 a.u. (窗口外)
lacO = 3: 无 IPTG → E = 0.04 a.u. (安全)   |   IPTG → E = 12.05 a.u. (最优)
lacO = 4: 无 IPTG → E = 0.03 a.u. (安全)   |   IPTG → E = 12.71 a.u. (最优)
lacO = 5: 无 IPTG → E = 0.02 a.u. (安全)   |   IPTG → E = 13.18 a.u. (最优)
```

**事实**：lacO = 3、4、5 **都**落在最优窗口内，而且 5 的表达水平更高。
因此 **"3×lacO 是最优"不成立**；模型支持的结论只是
**"lacO ≥ 3 即可同时满足低泄露与可激活"**。

**正确的表述**：
> "模型给出的**有效区间是 lacO ≥ 3**。我们选择 3 而不是 5，
> 依据是项目的**元件极简原则**——在满足功能的条件下取最少元件数，
> 这与我们'仅 2–3 个外源基因'的整体设计主张一致，而不是因为 3 在数学上最优。"

> ⚠️ 这一条很重要。申报书与 `docs/建模目的.docx` 都写了"3×lacO 能够较好地平衡安全性和功能性"，
> 并暗示它是唯一/最佳选择。**若被追问"为什么不是 4 或 5"，目前答不上来。**
> 按上面的表述改为"下界选择 + 极简原则"即可自洽。

---

### 9. ✅ 已修复：临界半衰期的打印结论与实际含义相反

**原代码**（遗留逻辑缺陷）：

```python
critical_idx = np.argmax(safety_window < 0)          # 若全为正，argmax 返回 0
critical_hl = half_lives[critical_idx] if critical_idx > 0 else half_lives[-1]
print(f"临界半衰期: {critical_hl:.1f} 分钟 (安全窗口归零)")
```

当扫描范围内安全窗口**始终为正**时，`argmax` 返回 0 → 走到 `half_lives[-1]`，
打印出 `临界半衰期: 60.0 分钟 (安全窗口归零)` ——
**文字与实际含义完全相反**（真实含义是"从未归零"）。

**已修复为**：用 `np.where(safety_window < 0)` 判断，区分"存在归零点"与"范围内未归零"两种情况，
输出分别对应不同文字。修改已同步到归档版与原始脚本。

---

## 四、结论强度的正确表述

| 结论 | ❌ 不能这么说 | ✅ 应这么说 |
|---|---|---|
| 振荡 | "工程菌能稳定振荡" | "在给定参数组合下，模型支持回路具备产生稳定振荡的动力学可行性" |
| 脉冲优于持续 | "脉冲给药疗效更好" | "等总剂量下，模型显示脉冲组的相对 TGI 高于持续组（58.6% vs 39.0%）" |
| 乳酸阈值 | "5 mM 以上就能启动" | "模型预测 ALPaGA 约在 5 mM 附近激活、5–6 mM 进入稳定振荡区，且存在稳态陷阱" |
| 3×lacO | "3 个 lacO 最优" / "3 个是平衡点" | "模型给出**有效区间是 lacO ≥ 3**；我们选 3 而非 5 的依据是**元件极简原则**（取满足功能的最少元件数），不是因为它数学上最优" |
| 安全窗口 | "系统在体内是安全的" | "模型支持'先关闭裂解、后清除细菌'的时序逻辑，且对蛋白半衰期鲁棒" |

### 另外需要注意的两处表述

`docs/建模目的.docx` 与申报书中出现的以下数字，**均为模型输出，不是实验结论**：

- "基础泄露降至 **2.9% 以下**"
- "正常器官细菌载量降低 **1–3 个数量级**"
- "脉冲组的肿瘤抑制率比持续组**高出 50%**"（对应模型输出 58.6% vs 39.0% 的相对比值）
- "将剩余 **95%** 的 pMMR/MSS 冷肿瘤转变为热肿瘤"（**这是愿景表述，不是模型结论**）

**被问到时，一律加一句："这是模型预测值，需要实验验证。"**

---

## 五、这个模型要怎样才算被证伪

一个模型如果不可能被证伪，就不是科学模型。本模型可被以下结果推翻：

1. OD600 长时间监测**观察不到 3 个以上连续的下降-恢复周期** → 弛豫振荡机制不成立；
2. 乳酸梯度实验中**不存在**"激活但不裂解"的中间区间 → 稳态陷阱不存在，LldR 自调控的必要性下降；
3. 改变 lacO 位点数**不改变**泄露水平 → LacI-lacO 阻尼设计无效；
4. 低乳酸条件下细菌**存活时间不短于**野生型 → asd 平衡致死系统未生效；
5. 等剂量荷瘤实验中脉冲组与持续组**无差异** → PK/PD-免疫节律模型的核心假设错误。

> **把这五条写出来，本身就是在展示科研素养。** 一个能说清"我的模型错在哪会被发现"的人，
> 比一个只会说"我的模型证明了什么"的人可信得多。

---

## 六、待办清单（按优先级）

### 必须做（对外表述前）

- [ ] 🔴 **统一安全窗口的物理量定义**（见 三-7）：确定第二个事件用 DAP 还是 Asd，
      或改为"Asd → DAP"两级串联；并同步修正 `docs/建模目的.docx` 中"约 30 分钟"的口径
- [ ] 🔴 **修正"3×lacO 最优"的表述**（见 三-8）：改为"lacO ≥ 3 均可，选 3 是极简原则"
- [ ] 🔴 补齐 `PARAMETERS.md` 中的文献出处（至少：`nb_EC50`、`ALPaGA EC50`、`Ki`、`k_death`）
- [ ] 🔴 统一 `src/04` 的 K50 公式（红/绿两组方向相反）与 12–15 / 12–20 窗口口径

### 应当做（提升模型可信度）

- [ ] 🔴 乳酸消耗与振荡的**闭环模型**（当前最大空缺，见 二-10）
- [ ] 🟡 为最终弛豫振荡设计写随机模拟版本（当前只有 QS 旧设计的随机版）
- [ ] 🟡 `src/06` 的时间步长收敛性检验
- [ ] 🟡 统一 `plt.savefig()` 输出到 `figs/`

### 可选（工程整洁度）

- [ ] 🟢 为所有脚本补充 `if __name__ == '__main__'` 保护

---

## 七、修复记录

| 日期 | 内容 | 位置 |
|---|---|---|
| 2026-09-17 | 删除图表人为缩放系数 `scale_factor = 0.85`，改为同指标内直接归一化 | `legacy/drug_efficacy_v3_simple.py` |
| 2026-09-17 | 指标名 `肿瘤抑制率` → `肿瘤回缩率`（因其相对初始值而非无治疗对照）；`np.mean` → 时间加权均值 | `legacy/drug_efficacy_v3_simple.py` |
| 2026-09-17 | 参数注释由"目标导向"改写为"取值依据" | `src/02_pkpd_immune_rhythm.py` |
| 2026-09-17 | 修复临界半衰期打印结论与实际含义相反的缺陷（见 三-9） | `src/05_safety_window_robustness.py` |
| 2026-09-17 | 更正 `Gillespie` 命名与文件头方法说明（非 SSA，且建模的是旧 QS 设计） | `src/06_stochastic_individual_based.py` |

> 原始注释与修改前内容完整保留在 [`HISTORY.md`](HISTORY.md) 的「代码整理时修改的内容」一节。
